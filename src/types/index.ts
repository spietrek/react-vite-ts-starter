export interface StatItem {
  pathD: string
  title: string
  count: string
  desc: string
}

export enum Severities {
  Info = 'info',
  Success = 'success',
  Warning = 'warning',
  Error = 'error',
}
